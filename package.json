{
  "name": "misc-sd-resources-v1",
  "version": "1.0.0",
  "description": "Misc SD resources and a Bootstrap UX starter served via a simple static dev server.",
  "private": true,
  "scripts": {
    "dev": "cd app && npx live-server --port=8000 --no-browser",
    "dev:open": "cd app && npx live-server --port=8000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}